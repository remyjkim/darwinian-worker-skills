---
name: hello-world
description: "Minimal example skill bundled with the minimal-card example."
---

# hello-world

This is an example skill bundled inside the `@your-handle/minimal-card`
example card. Replace `@your-handle` with your actual GitHub handle (or any
`@<scope>` you publish under) before running the publish flow described in
`examples/cards/README.md`.

When applied via `drwn apply @your-handle/minimal-card`, this skill
materializes under the project's downstream skill directories as plain
`hello-world` content resolved from the card store.

## What It Does

Says hello. That's it.

## Procedure

1. Greet the user.
